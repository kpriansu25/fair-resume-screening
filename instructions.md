# Fair Resume Screening App

## Run the app
```bash
source .venv/bin/activate
streamlit run app.py
```

## Notes
- Use `.venv/` (not `venv/`) — it is the active virtual environment
- If packages are missing: `pip install -r requirements.txt`
